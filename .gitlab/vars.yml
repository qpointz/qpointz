variables:
    CI_IMAGE_GRAALVM: ${CI_REGISTRY_IMAGE}/graalvm:${CI_COMMIT_REF_SLUG}-ci
    CI_IMAGE_GRADLE: ${CI_REGISTRY_IMAGE}/gradle:${CI_COMMIT_REF_SLUG}-ci
    CI_IMAGE_SBT: ${CI_REGISTRY_IMAGE}/sbt:${CI_COMMIT_REF_SLUG}-ci
    CI_IMAGE_NPM: ${CI_REGISTRY_IMAGE}/npm:${CI_COMMIT_REF_SLUG}-ci