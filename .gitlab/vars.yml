include:
  - local : /etc/docker/toolchain.yml

variables:        
    CI_IMAGE_BASE: ${CI_REGISTRY_IMAGE}/base-ci:${CI_TOOLCHAIN_ROLL}-${CI_COMMIT_REF_SLUG}
#    CI_IMAGE_GRAALVM: ${CI_REGISTRY_IMAGE}/graalvm-ci:${CI_TOOLCHAIN_ROLL}-${CI_COMMIT_REF_SLUG}
#    CI_IMAGE_GRADLE: ${CI_REGISTRY_IMAGE}/gradle-ci:${CI_TOOLCHAIN_ROLL}-${CI_COMMIT_REF_SLUG}
    CI_IMAGE_SBT: ${CI_REGISTRY_IMAGE}/sbt-ci:${CI_TOOLCHAIN_ROLL}-${CI_COMMIT_REF_SLUG}
#    CI_IMAGE_NPM: ${CI_REGISTRY_IMAGE}/npm-ci:${CI_TOOLCHAIN_ROLL}-${CI_COMMIT_REF_SLUG}
#    CI_IMAGE_PYTHON: ${CI_REGISTRY_IMAGE}/python-ci:${CI_TOOLCHAIN_ROLL}-${CI_COMMIT_REF_SLUG}
    CI_IMAGE_SPHINX: ${CI_REGISTRY_IMAGE}/sphinx-ci:${CI_TOOLCHAIN_ROLL}-${CI_COMMIT_REF_SLUG}
    CI_IMAGE_SEM_RELEASE: ${CI_REGISTRY_IMAGE}/semantic-release-qp:${CI_TOOLCHAIN_ROLL}-${CI_COMMIT_REF_SLUG}
    CI_IMAGE_MINICA: ${CI_REGISTRY_IMAGE}/minica-ci:${CI_TOOLCHAIN_ROLL}-${CI_COMMIT_REF_SLUG}
    
    