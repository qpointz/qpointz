/**
 * Brand mark image URL; file lives in Vite `public/` and is served under `import.meta.env.BASE_URL`.
 */
export const BRAND_LOGO_SRC = `${import.meta.env.BASE_URL}logo.png`;

/** Product name on auth screens and other static chrome (runtime app title may come from the server). */
export const BRAND_DISPLAY_NAME = 'Mill';
