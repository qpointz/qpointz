# Security

Operator-focused notes for authentication and related Mill configuration.

## Guides

| Topic | Description |
|-------|-------------|
| [Authentik (OIDC)](authentik-oidc.md) | Configure Mill Service and Mill UI to sign in with [Authentik](https://goauthentik.io/) using OpenID Connect (`oauth2Login` + JWT resource server). |

For local Authentik containers, blueprints, and discovery URLs used in development, see the repository design doc [`local-dev-authentik.md`](../../../design/platform/local-dev-authentik.md).
