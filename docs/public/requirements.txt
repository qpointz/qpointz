# MkDocs core
mkdocs>=1.6.0

# Material theme
mkdocs-material>=9.4.4

# Markdown extensions
pymdown-extensions>=10.12

# Syntax highlighting
Pygments>=2.0.0

# Version management
# Note: mike's dependencies (pyyaml, pyyaml-env-tag, jinja2, etc.) are automatically
# installed with mike or are provided by mkdocs
mike>=1.1.0

# Macros plugin
mkdocs-macros-plugin>=1.3.0

# Installation notes:
# 
# Option 1: Using pip (recommended for development)
#   pip install -r requirements.txt
#
# Option 2: Using pipx (recommended for system-wide installation)
#   sudo apt update && sudo apt install pipx python3-venv -y
#   pipx ensurepath
#   source ~/.bashrc  # or ~/.zshrc depending on your shell
#   pipx install mkdocs
#   pipx install mike
#   pipx inject mkdocs mkdocs-material "pymdown-extensions>=10.12" Pygments mkdocs-macros-plugin
