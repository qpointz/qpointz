# WI-265 — Integration tests + design doc + BACKEND requirements

Status: `planned`  
Type: `docs` / `feature`  
Area: `data`, `platform`  
Backlog refs: **D-8**, **U-13**

**Story:** [`STORY.md`](STORY.md) — **WI-265** (tracker row 4). **Depends on WI-262–WI-264** being merge-ready content-wise; this WI lands **Skymill `testIT`**, canonical design doc, and API doc sync (**one WI commit** per **RULES.md**).

## Tracker (this WI)

- [ ] **`docs/design/platform/query-result-execution-service.md`** — architecture + Skymill test notes + **HTTP table** matching **[`STORY.md`](STORY.md)** (`POST /api/v1/query`, **`GET /api/v1/query/{executionId}`** metadata, **`GET /api/v1/query/{executionId}/rows`** paging + optional **`format`** / **`epoch`**, **`DELETE`** = deallocation, **no** **`/executions`**); copy **Paging contract**, **Server and client buffering**, **Format negotiation**, and **Concurrency, invalidation, and replace** from **STORY** ( **`epoch`**, **`409`**, read–write ordering, **refill / live-data** consistency); include **response status matrix** aligned with **[`WI-264`](WI-264-query-result-rest-and-wiring.md)** **OpenAPI (controllers)**; **extension:** document **SPI** (**`ResultMarshallerProvider`**, **`META-INF/services`** path) and **Spring `ResultMarshallerRegistry`** assembly (**WI-263**); state **breaking** replacement of **`POST /api/v1/queries/execute`** / old **B-4** narrative for **mill-ui** execution (**no** deprecation section elsewhere); record **Kotlin** / **Java**-for-properties / **Skymill `testIT`** / **parameter-level** KDoc policy from **[`STORY.md`](STORY.md)** **Implementation conventions**
- [ ] **[`BACKEND-API-REQUIREMENTS.md`](../../../design/ui/mill-ui/BACKEND-API-REQUIREMENTS.md)** Queries domain: **`POST /api/v1/query`** (optional **`defaultFormat`**), **`GET /api/v1/query/{executionId}`** (metadata + **`epoch`**), **`GET /api/v1/query/{executionId}/rows`** (**`pageIndex`**, **`pageSize`**, optional **`format`**, optional **`epoch`**, **`Accept`**, envelope + **`data`** + **`epoch`**, **`Content-Type: application/json`** for built-ins), **`PUT`** replace (**`epoch`** increments), **`DELETE`** deallocates — **no** **`/executions`** segment; **no** public **`offset`/`limit`**; **`409`** stale **`epoch`**; **HTTP status codes** table consistent with **Springdoc** on controllers (**[`WI-264`](WI-264-query-result-rest-and-wiring.md)** **OpenAPI (controllers)**); **remove** normative **`POST /api/v1/queries/execute`** (**breaking** — **no** “deprecated” stub)
- [ ] **`docs/design` sweep (breaking)** — align with **[`STORY.md`](STORY.md)** **Story closure — reconcile `docs/design`** table: **[`BACKEND-BACKLOG.md`](../../../design/ui/mill-ui/BACKEND-BACKLOG.md)** (**G-10**, **B-4**), **[`ARCHITECTURE.md`](../../../design/ui/mill-ui/ARCHITECTURE.md)** (Queries / **`queryService`** row), **[`UI-ELEMENT-INVENTORY.md`](../../../design/ui/mill-ui/UI-ELEMENT-INVENTORY.md)** (**`queryService`** / analysis execute); same **STORY** list for export / codebase / client docs — **fix or remove** wrong HTTP path claims (**no** deprecation banners)
- [ ] Mirror **`ui/mill-ui/docs/BACKEND-API-REQUIREMENTS.md`** if it duplicates the canonical doc
- [ ] **`testIT` @ `skymill`** (schema + dataset under **`test/datasets/skymill/`**): **`flow-skymill-it.yaml`**, **`application-skymill.yml`**, **`skymill.datasets.dir`** **`systemProperty`** on **`testIT`** (same Gradle pattern as **[`mill-data-schema-core/build.gradle.kts`](../../../../data/mill-data-schema-core/build.gradle.kts)**); tests mirror **[`MillGrpcSkymillQueryIT`](../../../../services/mill-data-grpc-service/src/testIT/java/io/qpointz/mill/data/backend/grpc/MillGrpcSkymillQueryIT.java)**; expectations aligned with **[`test/it-querycases/skymill-sql.json`](../../../../test/it-querycases/skymill-sql.json)**; REST coverage includes **`GET …/rows`** **contract** cases (**`totalResult`** known vs unknown, partial last page **`rowCount < pageSize`**, backward **page 1 → page 0**), **format negotiation** (**`format=rows-objects`** vs **`rows-compact-batch`**, **`Content-Type`**, **`data`** shape, **`400`** unknown **`format`**, **`406`** bad **`Accept`**), **concurrency / invalidation** (**`PUT`** replace then **`GET …/rows?epoch=<before>`** → **`409`**; **`epoch`** in metadata matches **`/rows`**; **`DELETE`** or idle eviction then **`GET`** → **`404`**; where practical **concurrent read vs `replace`** ordering per **[`STORY.md`](STORY.md)** **Concurrency**), and **`403`** cross-user where practical
- [ ] **`./gradlew`** affected modules **`test`** + **`testIT`** green
- [ ] **WI-257** / [**U-13**](../../BACKLOG.md): saved-query **`GET /api/v1/queries`** unchanged; execution only under **`/api/v1/query`**; **`mill-ui`** **must** drop mock **`/queries/execute`** — **breaking**, **no** deprecation shim

## Goal

Finalize delivery documentation and coverage:

1. **`docs/design/platform/query-result-execution-service.md`** — architecture (**programmatic-first**, REST adapter, presentation **`pageIndex`/`pageSize`** vs internal **`executionBufferRows`**, **backward** server buffering, marshalling + **standard MIME** **`Content-Type`**, **`format`** / **`Accept`** / **`data`**, **`epoch`** + **`409`** + **`replace`** vs read serialization, eviction, **refill / live-data** weak consistency, horizontal scale); opening note that **`mill-data-query`** + **`mill-data-query-service`** and this HTTP model **replace** (**breaking**) older design that positioned **`POST /api/v1/queries/execute`** / **B-4** as the **mill-ui** execution path — **no** compatibility period.
2. Update **[`BACKEND-API-REQUIREMENTS.md`](../../../design/ui/mill-ui/BACKEND-API-REQUIREMENTS.md)** Queries domain: execution sessions at **`POST /api/v1/query`**, **`GET /api/v1/query/{executionId}`**, and **`GET /api/v1/query/{executionId}/rows`** (same path model as **[`STORY.md`](STORY.md)** **Paging contract**, **Format negotiation**, **Concurrency**); document **`DELETE /api/v1/query/{executionId}`** as explicit **deallocation**; full envelope + **`data`** + **`epoch`**; **`defaultFormat`** / **`format`** / **`Accept`** / **`400`**/**`406`**/**`409`**; **`PUT` replace**; **delete** normative **`POST /api/v1/queries/execute`** (**breaking** — see **[`GAPS.md`](GAPS.md) §1**).
3. **`docs/design` sweep** — every file under **Story closure — reconcile `docs/design`** in **[`STORY.md`](STORY.md)** **must** be updated so **`POST /api/v1/queries/execute`** does **not** appear as a supported or transitional **mill-service** / **mill-ui** contract (**no** “deprecated” sections, **no** dual primary routes).
4. **`testIT` against Skymill** — mirror **[`MillGrpcSkymillQueryIT`](../../../../services/mill-data-grpc-service/src/testIT/java/io/qpointz/mill/data/backend/grpc/MillGrpcSkymillQueryIT.java)**: **`@ActiveProfiles("skymill")`**, **`src/testIT/resources/flow-skymill-it.yaml`**, and **`application-skymill.yml`** under **`src/testIT/resources`** when the slice requires it; **`JvmTestSuite`** **`systemProperty("skymill.datasets.dir", rootProject.file("test/datasets/skymill").absolutePath)`** on the **`testIT`** **`testTask`** (same pattern as **[`mill-data-schema-core/build.gradle.kts`](../../../../data/mill-data-schema-core/build.gradle.kts)**). Use a **`@SpringBootTest`** configuration that exposes **`DataOperationDispatcher`** plus **`mill-data-query`** and **`mill-data-query-service`** beans; cover **`QueryResultExecutionService`** and, where practical, **`MockMvc`** / **`WebTestClient`** over **`POST /api/v1/query`**, **`GET /api/v1/query/{executionId}`**, **`GET /api/v1/query/{executionId}/rows`** (including **backward** page order, envelope + **`epoch`**, **format negotiation**, and **Concurrency** cases from **[`STORY.md`](STORY.md)**), **`PUT` replace**, **`DELETE /api/v1/query/{executionId}`**, and wrong-owner **`403`**. Prefer SQL and expectations aligned with—or drawn from—the shared corpus **[`test/it-querycases/skymill-sql.json`](../../../../test/it-querycases/skymill-sql.json)** for **`create`**, **`replace`**, paging window behaviour, **`VectorBlock`** reads, and marshaller/streaming paths.
5. **Spring configuration metadata** — **`@ConfigurationProperties`** implemented in **Java** with **`spring-boot-configuration-processor`** only (**no** Kotlin properties + manual **`additional-spring-configuration-metadata.json`** in these modules — **[`STORY.md`](STORY.md)** **Implementation conventions**).

## Scope

Pure docs + tests extending **WI-262**–**WI-264** as needed (**one commit** still reserved for **this WI** alone per **RULES.md** — keep code deltas minimal if prior WIs landed tests elsewhere).

## Acceptance

- `./gradlew` relevant modules **`test`** + **`testIT`** **green** (including **Skymill `testIT`** above).
- BACKEND doc describes **`/api/v1/query`** session API only; **`POST /api/v1/queries/execute`** is **absent** from normative text (**breaking**); **WI-257** scope stays saved-query **GET**s — execution client uses **`/api/v1/query/**` only.
- **`docs/design/platform/query-result-execution-service.md`** exists and is linked from reconciled **`docs/design/ui/mill-ui/**`** files where execution architecture is discussed.
- **`docs/design` sweep** (**BACKEND-BACKLOG**, **ARCHITECTURE**, **UI-ELEMENT-INVENTORY** minimum): **no** remaining normative **`/queries/execute`** path for **mill-ui**; **additional** **STORY**-listed files (export / codebase / client) **must not** imply a deprecated parallel route — **remove** or rewrite.
- Design doc + platform notes mention **Kotlin** implementation, **Java**-only **`@ConfigurationProperties`**, **Skymill `testIT`**, and **parameter-level** KDoc/JavaDoc policy.

## Depends on

**WI-264**

## Notes

**MILESTONE.md** / full **story closure** per **RULES.md** occurs when **all** story WIs are done and MR-ready.
