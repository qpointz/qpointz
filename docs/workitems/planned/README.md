# Planned stories

Stories live here while **every** work-item checkbox in `STORY.md` is still **`[ ]`**.

When the **first** WI is marked `[x]`, move the entire `<story-slug>/` folder to
[`../in-progress/`](../in-progress/) (same commit or PR).

Normative rules: [`../RULES.md`](../RULES.md) — **Story folder layout** and **Placement rule (checkbox-based)**.
