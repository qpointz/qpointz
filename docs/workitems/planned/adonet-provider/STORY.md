# Managed ADO.NET Provider

Deliver a first-party managed ADO.NET provider for Mill with dual transport support (gRPC and
HTTP), read-only query execution, `DbDataReader`-based result consumption, schema metadata
discovery, and authentication/TLS parity with existing clients.

## Work Items

- [ ] WI-077 — Managed ADO.NET Provider for Mill (`WI-077-adonet-provider.md`)
