# Gradle Multi-Edition Build

Create a reusable Gradle convention plugin in `build-logic` that enables predefined edition
builds for Spring Boot distributions, without introducing multiple app modules or `shadowJar`.

## Work Items

- [ ] WI-014 — Gradle Multi-Edition BootDist Plugin (`WI-014-gradle-multi-edition-bootdist-plugin.md`)
