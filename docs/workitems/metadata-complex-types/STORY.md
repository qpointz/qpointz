# Metadata — Complex Type Support

Extend metadata structural contracts and UI rendering so complex and nested types (`LIST`, `MAP`,
`OBJECT`) are represented consistently across repository, service, and browser layers once the
underlying data-layer complex type work lands.

**Blocked by:** data-layer complex type work items `D-2`, `D-3`, `D-4`.

## Work Items

- [ ] WI-034 — Metadata Complex Type Support in Structural Facets and UI (`WI-034-metadata-complex-type-support.md`)
