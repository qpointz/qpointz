package io.qpointz.mill.events.model

/**
 * Marker interface for event payload types.
 *
 * Each domain event defines a concrete implementation carrying the relevant data.
 */
interface EventPayload
