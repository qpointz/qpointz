logging:
  level:
    root: debug

grpc:
  server:
    in-process-name: jdbc-test-service
    port: -1

mill:
  data:
    sql:
      dialect: calcite
      conventions:
        quoting: BACK_TICK
        caseSensitive: true
        unquotedCasing: UNCHANGED
    backend:
      type: calcite
      calcite:
        model: ./etc/config/test/model.yaml
  security:
    enable: false
  services:
    grpc:
      enable: true
