name: trivial-scenario #sceanrio name
run: #list of actions to run
  #each action is composition of steps
  - ask: list clients
  - verify:
      message: 0
      check:
        - intent: get-data
        - has:
            - sql
            - data
  - ask: count clients in Korea
  - verify:
      message: 0
      check:
        - intent: get-data
        - has:
            - sql
            - data