<?xml version="1.0" encoding="UTF-8"?>
<!--
  Console: colored by default; plain when QP_LOGGING_PLAIN=true.
  File: when logging.file.enabled=true and logging.file.name is set (default local path in application.yml).
  Disable file logs: logging.file.enabled=false and/or omit logging.file.name (safe on read-only filesystems).
  Cloud Run (profile gcp): structured JSON to stdout only — see deploy/gcp/blueprints/micro/config/application.tpl.yml.
  CONSOLE uses withJansi=false so Logback writes ANSI directly (IDE-friendly despite NO_COLOR in the shell).
-->
<configuration debug="${QP_LOGGING_DEBUG:-false}">
    <statusListener class="ch.qos.logback.core.status.NopStatusListener"/>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProperty name="CONSOLE_LOG_PATTERN" source="logging.pattern.console"
                    defaultValue="%gray(|) %cyan(%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}}) %gray(|) %yellow(%-15.15thread) %gray(|) %highlight(%-5level) %gray(|) %magenta(%-28.28logger{28}) %gray(|) %msg%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>
    <springProperty name="PLAIN_CONSOLE_LOG_PATTERN" source="logging.pattern.plain-console"
                    defaultValue="%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} | %-15.15thread | ${LOG_LEVEL_PATTERN:-%-5level} | %-28.28logger{28} | %msg%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>
    <springProperty name="FILE_LOG_PATTERN" source="logging.pattern.file"
                    defaultValue="%d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} | %-15.15thread | ${LOG_LEVEL_PATTERN:-%-5level} | %-28.28logger{28} | %msg%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <springProperty name="LOG_FILE" source="logging.file.name"/>
    <springProperty name="LOGGING_FILE_ENABLED" source="logging.file.enabled" defaultValue="true"/>
    <springProperty name="ROOT_LOG_LEVEL" source="logging.level.root" defaultValue="${QP_LOGGING_LEVEL:-INFO}"/>

    <!-- Google Cloud Run (gcp profile): JSON stdout for Cloud Logging severity + jsonPayload. -->
    <springProfile name="gcp">
        <springProperty name="CONSOLE_LOG_STRUCTURED_FORMAT" source="logging.structured.format.console"
                        defaultValue="logstash"/>
        <appender name="CONSOLE_STRUCTURED" class="ch.qos.logback.core.ConsoleAppender">
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>${CONSOLE_LOG_THRESHOLD}</level>
            </filter>
            <encoder class="org.springframework.boot.logging.logback.StructuredLogEncoder">
                <format>${CONSOLE_LOG_STRUCTURED_FORMAT}</format>
                <charset>${CONSOLE_LOG_CHARSET}</charset>
            </encoder>
        </appender>

        <root level="${ROOT_LOG_LEVEL}">
            <appender-ref ref="CONSOLE_STRUCTURED"/>
        </root>
    </springProfile>

    <!-- Local and other profiles: console + optional rolling file. -->
    <springProfile name="!gcp">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <withJansi>false</withJansi>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>${CONSOLE_LOG_THRESHOLD}</level>
            </filter>
            <encoder>
                <pattern>${CONSOLE_LOG_PATTERN}</pattern>
                <charset>${CONSOLE_LOG_CHARSET}</charset>
            </encoder>
        </appender>

        <appender name="CONSOLE_PLAIN" class="ch.qos.logback.core.ConsoleAppender">
            <withJansi>false</withJansi>
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>${CONSOLE_LOG_THRESHOLD}</level>
            </filter>
            <encoder>
                <pattern>${PLAIN_CONSOLE_LOG_PATTERN}</pattern>
                <charset>${CONSOLE_LOG_CHARSET}</charset>
            </encoder>
        </appender>

        <if condition='property("LOGGING_FILE_ENABLED").equalsIgnoreCase("true") &amp;&amp; property("LOG_FILE").length() &gt; 0'>
            <then>
                <include resource="org/springframework/boot/logging/logback/file-appender.xml"/>
            </then>
        </if>

        <condition class="ch.qos.logback.core.boolex.PropertyEqualityCondition">
            <key>QP_LOGGING_PLAIN</key>
            <value>true</value>
        </condition>
        <if>
            <then>
                <if condition='property("LOGGING_FILE_ENABLED").equalsIgnoreCase("true") &amp;&amp; property("LOG_FILE").length() &gt; 0'>
                    <then>
                        <root level="${ROOT_LOG_LEVEL}">
                            <appender-ref ref="CONSOLE_PLAIN"/>
                            <appender-ref ref="FILE"/>
                        </root>
                    </then>
                    <else>
                        <root level="${ROOT_LOG_LEVEL}">
                            <appender-ref ref="CONSOLE_PLAIN"/>
                        </root>
                    </else>
                </if>
            </then>
            <else>
                <if condition='property("LOGGING_FILE_ENABLED").equalsIgnoreCase("true") &amp;&amp; property("LOG_FILE").length() &gt; 0'>
                    <then>
                        <root level="${ROOT_LOG_LEVEL}">
                            <appender-ref ref="CONSOLE"/>
                            <appender-ref ref="FILE"/>
                        </root>
                    </then>
                    <else>
                        <root level="${ROOT_LOG_LEVEL}">
                            <appender-ref ref="CONSOLE"/>
                        </root>
                    </else>
                </if>
            </else>
        </if>
    </springProfile>
</configuration>
