# Spring 3 reference trees

This directory holds **reference-only** snapshots (for example the historical **net.devh** gRPC layout under `mill-data-grpc-service`); it is **not** part of the root composite build in `settings.gradle.kts` and is **not** shipped as product code.
