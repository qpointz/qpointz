# Azure Functions sample (`mill-azure-service-function`)

This tree is **archive / reference** material. It is **not** included in the root **`settings.gradle.kts`** composite and is **not** on the main CI build graph.

The `build.gradle.kts` still references historical Gradle project names (`:mill-starter-backends`, `:mill-common-security`) that do not exist in the current Mill layout; treat the project as **documentation-only** unless someone repairs the graph for a standalone experiment.
