qp:
  mill:
    backend:
      calcite:
        connection:
          model: ./config/model.yaml
      security:
        enabled: true
        providers:
          - type: file
            path: ./config/passwd.yaml
logging:
  level:
    root: info